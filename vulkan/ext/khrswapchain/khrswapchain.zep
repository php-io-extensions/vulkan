// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace Vulkan\Ext\KHRSwapchain;

class KHRSwapchain
{
    public static function vkCreateSwapchainKHR(int device, int pCreateInfo, int pAllocator, int pSwapchain) -> int
    {
        return khrswapchain_vkcreateswapchainkhr(device, pCreateInfo, pAllocator, pSwapchain);
    }

    public static function vkDestroySwapchainKHR(int device, int swapchain, int pAllocator) -> void
    {
        khrswapchain_vkdestroyswapchainkhr(device, swapchain, pAllocator);
    }

    public static function vkGetSwapchainImagesKHR(int device, int swapchain, int pSwapchainImageCount, int pSwapchainImages) -> int
    {
        return khrswapchain_vkgetswapchainimageskhr(device, swapchain, pSwapchainImageCount, pSwapchainImages);
    }

    public static function vkAcquireNextImageKHR(int device, int swapchain, int timeout, int semaphore, int fence, int pImageIndex) -> int
    {
        return khrswapchain_vkacquirenextimagekhr(device, swapchain, timeout, semaphore, fence, pImageIndex);
    }

    public static function vkQueuePresentKHR(int queue, int pPresentInfo) -> int
    {
        return khrswapchain_vkqueuepresentkhr(queue, pPresentInfo);
    }

    public static function vkGetDeviceGroupPresentCapabilitiesKHR(int device, int pDeviceGroupPresentCapabilities) -> int
    {
        return khrswapchain_vkgetdevicegrouppresentcapabilitieskhr(device, pDeviceGroupPresentCapabilities);
    }

    public static function vkGetDeviceGroupSurfacePresentModesKHR(int device, int surface, int pModes) -> int
    {
        return khrswapchain_vkgetdevicegroupsurfacepresentmodeskhr(device, surface, pModes);
    }

    public static function vkGetPhysicalDevicePresentRectanglesKHR(int physicalDevice, int surface, int pRectCount, int pRects) -> int
    {
        return khrswapchain_vkgetphysicaldevicepresentrectangleskhr(physicalDevice, surface, pRectCount, pRects);
    }

    public static function vkAcquireNextImage2KHR(int device, int pAcquireInfo, int pImageIndex) -> int
    {
        return khrswapchain_vkacquirenextimage2khr(device, pAcquireInfo, pImageIndex);
    }
}
