// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace Vulkan\VK\VK12;

class VK12
{
    public static function vkResetQueryPool(int device, int queryPool, int firstQuery, int queryCount) -> void
    {
        vk12_vkresetquerypool(device, queryPool, firstQuery, queryCount);
    }

    public static function vkGetSemaphoreCounterValue(int device, int semaphore, int pValue) -> int
    {
        return vk12_vkgetsemaphorecountervalue(device, semaphore, pValue);
    }

    public static function vkWaitSemaphores(int device, int pWaitInfo, int timeout) -> int
    {
        return vk12_vkwaitsemaphores(device, pWaitInfo, timeout);
    }

    public static function vkSignalSemaphore(int device, int pSignalInfo) -> int
    {
        return vk12_vksignalsemaphore(device, pSignalInfo);
    }

    public static function vkGetBufferDeviceAddress(int device, int pInfo) -> int
    {
        return vk12_vkgetbufferdeviceaddress(device, pInfo);
    }

    public static function vkGetBufferOpaqueCaptureAddress(int device, int pInfo) -> int
    {
        return vk12_vkgetbufferopaquecaptureaddress(device, pInfo);
    }

    public static function vkGetDeviceMemoryOpaqueCaptureAddress(int device, int pInfo) -> int
    {
        return vk12_vkgetdevicememoryopaquecaptureaddress(device, pInfo);
    }

    public static function vkCmdDrawIndirectCount(int commandBuffer, int buffer, int offset, int countBuffer, int countBufferOffset, int maxDrawCount, int stride) -> void
    {
        vk12_vkcmddrawindirectcount(commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    }

    public static function vkCmdDrawIndexedIndirectCount(int commandBuffer, int buffer, int offset, int countBuffer, int countBufferOffset, int maxDrawCount, int stride) -> void
    {
        vk12_vkcmddrawindexedindirectcount(commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    }

    public static function vkCreateRenderPass2(int device, int pCreateInfo, int pAllocator, int pRenderPass) -> int
    {
        return vk12_vkcreaterenderpass2(device, pCreateInfo, pAllocator, pRenderPass);
    }

    public static function vkCmdBeginRenderPass2(int commandBuffer, int pRenderPassBegin, int pSubpassBeginInfo) -> void
    {
        vk12_vkcmdbeginrenderpass2(commandBuffer, pRenderPassBegin, pSubpassBeginInfo);
    }

    public static function vkCmdNextSubpass2(int commandBuffer, int pSubpassBeginInfo, int pSubpassEndInfo) -> void
    {
        vk12_vkcmdnextsubpass2(commandBuffer, pSubpassBeginInfo, pSubpassEndInfo);
    }

    public static function vkCmdEndRenderPass2(int commandBuffer, int pSubpassEndInfo) -> void
    {
        vk12_vkcmdendrenderpass2(commandBuffer, pSubpassEndInfo);
    }
}
